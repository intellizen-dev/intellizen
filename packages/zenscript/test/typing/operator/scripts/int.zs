val one = 1 as int;

// unary
    -one;
    !one;

// binary
    // bitwise
    one & one;
    one | one;
    one ^ one;

    // range
    one .. one;
    one to one;

    // arithmetic
    one + one;
    one - one;
    one * one;
    one / one;
    one % one;

    // comparison
    one < one;
    one > one;
    one <= one;
    one >= one;
    one == one;
    one != one;

    // assignment
    one &= one;
    one |= one;
    one ^= one;

    one += one;
    one -= one;
    one *= one;
    one /= one;
    one %= one;
