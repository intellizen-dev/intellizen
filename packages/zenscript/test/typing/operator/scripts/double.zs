val one = 1.0D as double;

// unary
    -one;

// binary
    // arithmetic
    one + one;
    one - one;
    one * one;
    one / one;
    one % one;

    // comparison
    one < one;
    one > one;
    one <= one;
    one >= one;
    one == one;
    one != one;

    // assignment
    one += one;
    one -= one;
    one *= one;
    one /= one;
    one %= one;
