val one = 1 as byte;

// unary
    -one;
    !one;

// binary
    // bitwise
    one & one;
    one | one;
    one ^ one;

    // arithmetic
    one + one;
    one - one;
    one * one;
    one / one;
    one % one;

    // comparison
    one < one;
    one > one;
    one <= one;
    one >= one;
    one == one;
    one != one;

    // assignment
    one &= one;
    one |= one;
    one ^= one;

    one += one;
    one -= one;
    one *= one;
    one /= one;
    one %= one;
