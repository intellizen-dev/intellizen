val one = 1.0f as float;

// unary
    -one;

// binary
    // arithmetic
    one + one;
    one - one;
    one * one;
    one / one;
    one % one;

    // comparison
    one < one;
    one > one;
    one <= one;
    one >= one;
    one == one;
    one != one;

    // assignment
    one &= one;
    one |= one;
    one ^= one;

    one += one;
    one -= one;
    one *= one;
    one /= one;
    one %= one;
