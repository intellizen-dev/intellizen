{
  "name": "@intellizen/zenscript",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "rimraf src/generated && wait-on src/generated && pnpm build --watch",
    "langium:generate": "langium generate",
    "langium:generate:watch": "pnpm langium:generate --watch"
  },
  "dependencies": {
    "langium": "^3.1.3",
    "vscode-languageserver": "^9.0.1"
  },
  "devDependencies": {
    "langium-cli": "^3.1.1"
  }
}
