package;
zenClass Map<K, V> {
    val length as int;

    val keys as K[];

    val values as V[];

    val keySet as K[];

    val valueSet as V[];

    val entrySet as Entry<K, V>;

    operator has(key as any) as bool;

    operator [](key as K) as V;

    operator []=(key as K, value as V) as void;

    operator for(key as K) as void;

    operator for(key as K, value as V) as void;

    operator .(key as string) as V;
}
