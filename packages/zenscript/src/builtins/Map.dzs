zenClass Map<K, V> {
    val length as int;

    val keys as [K];

    val values as [V];

    val keySet as [K];

    val valueSet as [V];

    val entrySet as Entry<K, V>;

    operator for_1() as K;

    operator for_2() as K, V;
}
